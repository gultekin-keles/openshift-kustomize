apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: web-pay

resources:
  - ../../components/nginx/base

replicas:
  - name: nginx
    count: 3

commonLabels:
  environment: prod

configMapGenerator:
  - name: index-html
    files:
      - index.html

