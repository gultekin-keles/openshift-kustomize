apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: vcard-base

resources:
- vcard-deployment.yaml
- vcard-service.yaml
- vcard-route.yaml

labels:
- pairs:
    app: vcard
