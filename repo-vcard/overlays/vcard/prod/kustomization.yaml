apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: vcard-prod

resources:
- ../../../components/vcard/base

replicas:
- name: vcard
  count: 3

labels:
- includeSelectors: true
  pairs:
    environment: prod
