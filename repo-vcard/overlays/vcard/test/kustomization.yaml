apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: vcard-test

resources:
- ../../../components/vcard/base

namePrefix: test-
nameSuffix: -test

replicas:
- name: vcard
  count: 1

labels:
- includeSelectors: true
  pairs:
    environment: test
