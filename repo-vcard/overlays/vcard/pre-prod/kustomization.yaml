apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: vcard-pre-prod

resources:
- ../../../components/vcard/base

namePrefix: preprod-
nameSuffix: -preprod

replicas:
- name: vcard
  count: 2

labels:
- includeSelectors: true
  pairs:
    environment: pre-prod
